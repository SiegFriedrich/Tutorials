const arr= [];
for(let i = 0; i<10; i ++){
arr.push(Math.floor(Math.random()*100));
}
console.log(arr);
const newarr = [];
for(let r = 0; r<arr.length; r ++){
    let t = Math.max(arr);
    console.log(t);
    arr.pop(t);
    newarr.push(t);
}
document.write(newarr);
